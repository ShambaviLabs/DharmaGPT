[pytest]
testpaths = tests
asyncio_mode = auto

markers =
    unit: fast offline tests — no API keys required, run on every commit
    integration: offline local-model integration tests using Ollama and the seed corpus
    smoke: fast application startup and route availability checks
    contract: API route, response-shape, and auth-boundary contract checks
