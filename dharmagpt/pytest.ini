[pytest]
testpaths = tests
asyncio_mode = auto

markers =
    unit: fast offline tests — no API keys required, run on every commit
    integration: full pipeline tests — require ANTHROPIC_API_KEY, OPENAI_API_KEY, PINECONE_API_KEY
